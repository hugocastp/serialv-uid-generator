{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "extensionHost",
            "request": "launch",
            "name": "Debug Extension",
            "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
        }
    ]
}
